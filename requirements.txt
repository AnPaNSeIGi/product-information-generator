# 乐天商品上架自动化系统依赖
# 安装命令：pip install -r requirements.txt

# Excel 读写
openpyxl>=3.0.0

# 数据处理
pandas>=1.3.0

# 阿里云 DashScope SDK（调用通义千问 VL）
dashscope>=1.14.0

# 自然排序（处理文件名）
natsort>=8.0.0

# 图片处理（Pillow）
Pillow>=9.0.0

# 所有依赖均为公开 PyPI 包，可直接通过 pip 安装。

# 版本号指定了最低兼容版本，更高的版本通常也可以工作。

# 如果使用 Python 3.8 及以上，这些依赖均能正常安装。

# 安装命令
# pip install -r requirements.txt

# 如果需要在中国大陆加速下载，可使用镜像源：
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple